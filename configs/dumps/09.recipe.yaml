# Generated by kalfa 0.2.4 from configs/09_simclr.yaml: the driver document kalfa.driver.recipe produces,
# the one the templates of src/kalfa/templates/kalfa.yaml open. Not written by hand; regenerate with:
#   uv run python tests/fixtures/regenerate_recipes.py
# cirak's fixture copy replaces the include with kalfa.yaml and regenerates configs/dumps/*.flow.yaml.
include: [../../src/kalfa/templates/kalfa.yaml]
losses:
  ntx: {uri: /objective/kalfa/ntxent, params: {model: simclr, temperature: 0.5}}
metrics: {}
triggers: {}
plots:
  loss_curve: {uri: /plot/kalfa/loss_curve}
progress: {uri: /lego/kalfa/progress}
blocks:
  backbone:
    inputs: [image]
    outputs: [h]
    spec:
    - {uri: /layer/timm/timm_backbone, params: {name: resnet18, pooled: true}}
  head:
    inputs: [h]
    outputs: [z]
    spec:
    - {uri: /layer/kalfa/linear_relu, params: {out_features: 512}}
    - {uri: /layer/kalfa/linear, params: {out_features: 128}}
  simclr:
    inputs: [image]
    outputs: [z]
    graph:
      h: {model: backbone, inputs: [image]}
      z: {model: head, inputs: [h]}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/image_folder, params: {path: data/stl10/unlabeled}}, 
        filter_pre: [], filter_set: [], split: {uri: /split/kalfa/random, params: {ratios: [1.0, 0.0, 0.0], seed: 7}}, 
        batch: {size: 256, drop_last: true}, preprocessors: {simclr_aug: {uri: /pre/kalfa/simclr_aug, params: {size: 96}},
        two_views: {uri: /pre/kalfa/two_views, params: {transform: {uri: /pre/kalfa/simclr_aug, params: {size: 96}}}}, 
          to_tensor: {uri: /pre/kalfa/to_tensor}}, preprocessors_keys: {simclr_aug: {}, two_views: {sets: [train]}, 
          to_tensor: {}}, drop: [], fields: {image: {preprocessors: [two_views, to_tensor]}}, feed: {uri: 
          /feed/kalfa/table, params: {}}}}
  models: {block: models, params: {trained_items: [{name: backbone, index: 0, init: null, trainable: true, weights: null},
        {name: head, index: 1, init: null, trainable: true, weights: null}], composite_items: [name: simclr], ema_items: [],
      trained_refs: {backbone: backbone, head: head}, composite_refs: {simclr: simclr}, ema_refs: {}, seed: 7}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: main, uri: /optimizer/torch/adamw, params: {lr: 
              0.0003}, loss: ntx, schedule: null, models: {backbone: backbone, head: head}}], optimizer_refs: {main: 
          opt_main}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {}}, turn_params: {amp: true}, 
        losses_keys: {ntx: {sets: [train]}}, metrics_keys: {}, predicts: simclr, epochs: 100, steps: null, rules: [], 
        stop: [], checkpoint: {uri: /checkpoint/kalfa/last}}}
  after: {block: after, params: {report: last, figures: null, predicts: simclr, targets: {}, generate: null, plots_keys: {
        loss_curve: {}}}}
