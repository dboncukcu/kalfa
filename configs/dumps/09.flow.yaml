# Generated by kalfa 0.2.0 (cirak 0.2.0) from configs/09_simclr.yaml: kalfa check --dump, the flow the
# templates of src/kalfa/templates/kalfa.yaml open for the driver document configs/dumps/09.recipe.yaml.
# Regenerate with: uv run python tests/fixtures/regenerate_dumps.py
# Sections: components (the recipe's component tables), blocks (spec and graph blocks as templates;
# the flow blocks are opened inside flow), flow (the expanded flow with every output written out;
# the comments are tezgah's resolution: implicit bindings, pattern matches, derived reads and exports).
components:
  losses:
    ntx: {uri: /objective/kalfa/ntxent, params: {model: simclr, temperature: 0.5}}
  metrics: {}
  triggers: {}
  plots:
    loss_curve: {uri: /plot/kalfa/loss_curve}
  progress: {uri: /lego/kalfa/progress}
blocks:
  backbone:
    inputs: [image]
    outputs: [h]
    spec:
    - {uri: /layer/timm/timm_backbone, params: {name: resnet18, pooled: true}}
  head:
    inputs: [h]
    outputs: [z]
    spec:
    - {uri: /layer/kalfa/linear_relu, params: {out_features: 512}}
    - {uri: /layer/kalfa/linear, params: {out_features: 128}}
  simclr:
    inputs: [image]
    outputs: [z]
    graph:
      h: {model: backbone, inputs: [image]}
      z: {model: head, inputs: [h]}
flow:
  outputs: [history, predictions]
  data:  # reads: record; exports: prep, train_loader, valid_loader, test_loader
    source: {uri: /source/kalfa/image_folder, params: {path: data/stl10/unlabeled}, outputs: [df_0]}
    split: {uri: /split/kalfa/random, params: {ratios: [1.0, 0.0, 0.0], seed: 7}, inputs: {df: df_0}, outputs: {train: train_df_0, valid: valid_df_0, test: 
          test_df_0}}
    set_filter_train: {uri: /lego/kalfa/filter_set, params: {set: train, filters: []}, inputs: {df: train_df_0}, outputs: [train_df]}
    set_filter_valid: {uri: /lego/kalfa/filter_set, params: {set: valid, filters: []}, inputs: {df: valid_df_0}, outputs: [valid_df]}
    set_filter_test: {uri: /lego/kalfa/filter_set, params: {set: test, filters: []}, inputs: {df: test_df_0}, outputs: [test_df]}
    prep: {uri: /lego/kalfa/fit, params: {fields: {image: {preprocessors: [two_views, to_tensor]}}, preprocessors: {simclr_aug: {uri: /pre/kalfa/simclr_aug, 
              params: {size: 96}}, two_views: {uri: /pre/kalfa/two_views, params: {transform: {uri: /pre/kalfa/simclr_aug, params: {size: 96}}}}, to_tensor: {
              uri: /pre/kalfa/to_tensor}}, drop: [], keys: {simclr_aug: {}, two_views: {sets: [train]}, to_tensor: {}}}, inputs: {df: train_df}, outputs: [prep]}  # implicit: record
    frame_train: {uri: /lego/kalfa/apply, params: {set: train, keys: {simclr_aug: {}, two_views: {sets: [train]}, to_tensor: {}}}, inputs: {df: train_df, prep: 
          prep}, outputs: [train_frame]}
    frame_valid: {uri: /lego/kalfa/apply, params: {set: valid, keys: {simclr_aug: {}, two_views: {sets: [train]}, to_tensor: {}}}, inputs: {df: valid_df, prep: 
          prep}, outputs: [valid_frame]}
    frame_test: {uri: /lego/kalfa/apply, params: {set: test, keys: {simclr_aug: {}, two_views: {sets: [train]}, to_tensor: {}}}, inputs: {df: test_df, prep: 
          prep}, outputs: [test_frame]}
    feed_train: {uri: /feed/kalfa/table, inputs: {frame: train_frame, frames: {train: train_frame, valid: valid_frame, test: test_frame}}, outputs: [train_data]}
    feed_valid: {uri: /feed/kalfa/table, inputs: {frame: valid_frame, frames: {train: train_frame, valid: valid_frame, test: test_frame}}, outputs: [valid_data]}
    feed_test: {uri: /feed/kalfa/table, inputs: {frame: test_frame, frames: {train: train_frame, valid: valid_frame, test: test_frame}}, outputs: [test_data]}
    loader_train: {uri: /loader/kalfa/torch, params: {set: train, batch: {size: 256, drop_last: true}}, inputs: {data: train_data}, outputs: [train_loader]}
    loader_valid: {uri: /loader/kalfa/torch, params: {set: valid, batch: {size: 256, drop_last: true}}, inputs: {data: valid_data}, outputs: [valid_loader]}
    loader_test: {uri: /loader/kalfa/torch, params: {set: test, batch: {size: 256, drop_last: true}}, inputs: {data: test_data}, outputs: [test_loader]}
  models: # reads: prep, train_loader; exports: backbone, head, models, emas, composites
    build_backbone: {block: backbone, builder: /builder/kalfa/module, params: {seed: 7, index: 0, init: null, trainable: true, weights: null}, outputs: [
          backbone]}  # implicit: prep, train_loader
    build_head: {block: head, builder: /builder/kalfa/module, params: {seed: 7, index: 1, init: null, trainable: true, weights: null}, outputs: [head]} # implicit: prep, train_loader
    models: {uri: /lego/kalfa/pack, inputs: {items: {backbone: backbone, head: head}}, outputs: [models]}
    emas: {uri: /lego/kalfa/pack, inputs: {items: {}}, outputs: [emas]}
    compose_simclr: {block: simclr, builder: /builder/kalfa/module, inputs: {models: models}, outputs: [simclr]} # implicit: prep, train_loader
    composites: {uri: /lego/kalfa/pack, inputs: {items: {simclr: simclr}}, outputs: [composites]}
  optimizers: # reads: backbone, head; exports: optimizers
    build_opt_main: {uri: /optimizer/torch/adamw, params: {params: {lr: 0.0003}, schedule: null, loss: ntx}, inputs: {models: {backbone: backbone, head: head}}, 
        outputs: [opt_main]}
    optimizers: {uri: /lego/kalfa/pack, inputs: {items: {main: opt_main}}, outputs: [optimizers]}
  training: # reads: models, optimizers, emas, device, composites, train_loader, prep, record, valid_loader, test_loader; exports: history, models_final, optimizers_final, emas_final, counters_final, rules_final
    counters: {uri: /lego/kalfa/const, params: {value: {global_step: 0, turn: 0}}, outputs: [counters]}
    rules: {uri: /lego/kalfa/const, params: {value: {}}, outputs: [rules]}
    init: {uri: /lego/kalfa/init_state, params: {epochs: 100, steps: null}, inputs: {state: {models: models, optimizers: optimizers, emas: emas, counters: 
            counters, rules: rules}}, outputs: [epochs_left]}  # implicit: device
    epochs: # reads: composites, train_loader, device, prep, record, valid_loader, test_loader
      loop:
        carry: [models, optimizers, emas, counters, rules]
        next: _next
        range: epochs_left
        index: turn_index
        until: stop
        trace:
          metrics: history
        outputs:
          models: models_final
          optimizers: optimizers_final
          emas: emas_final
          counters: counters_final
          rules: rules_final
        body:  # reads: rules, models, optimizers, emas, counters, composites, train_loader, device, prep, record, valid_loader, test_loader, turn_index; exports: models_next, optimizers_next, emas_next, counters_next, metrics, rules_next, stop
          effects: {uri: /rule/kalfa/effects, inputs: {rules: rules}, outputs: [effects]}
          turn: {uri: /turn/kalfa/alternating, params: {params: {}, extra: {amp: true}, losses: '@losses', metrics: '@metrics', losses_keys: {ntx: {sets: [train]}},
              metrics_keys: {}, predicts: simclr, steps: null}, inputs: {models: models, optimizers: optimizers, emas: emas, counters: counters, composites: 
                composites, effects: effects, loader: train_loader}, outputs: {models: models_next, optimizers: optimizers_next, emas: emas_next, counters: 
                counters_next, metrics: train_metrics}}  # implicit: device, prep, record
          evaluate_valid: {uri: /lego/kalfa/evaluate, params: {set: valid, losses: '@losses', metrics: '@metrics', losses_keys: {ntx: {sets: [train]}}, 
                metrics_keys: {}, predicts: simclr}, inputs: {models: models_next, emas: emas_next, composites: composites, counters: counters_next, effects: 
                effects, loader: valid_loader}, outputs: [valid_metrics]} # implicit: device, prep, record
          evaluate_test: {uri: /lego/kalfa/evaluate, params: {set: test, losses: '@losses', metrics: '@metrics', losses_keys: {ntx: {sets: [train]}}, 
                metrics_keys: {}, predicts: simclr}, inputs: {models: models_next, emas: emas_next, composites: composites, counters: counters_next, effects: 
                effects, loader: test_loader}, outputs: [test_metrics]} # implicit: device, prep, record
          metrics: {uri: /lego/kalfa/merge, inputs: {parts: '*_metrics'}, outputs: [metrics]} # parts *_metrics: test_metrics, train_metrics, valid_metrics
          rules_0: {uri: /rule/kalfa/open, inputs: {rules: rules}, outputs: [rules_0]}
          rules_ruled: {uri: /lego/kalfa/identity, inputs: {value: rules_0}, outputs: [rules_ruled]}
          stop: {uri: /rule/kalfa/stop, params: {triggers: []}, inputs: {rules: rules_ruled}, outputs: {rules: rules_next, stop: stop}}  # implicit: metrics
          checkpoint: {uri: /lego/kalfa/checkpoint, params: {policy: {uri: /checkpoint/kalfa/last}}, inputs: {state: '*_next'}, outputs: []} # state *_next: counters_next, emas_next, models_next, optimizers_next, rules_next; implicit: metrics, record
          log: {uri: /lego/kalfa/history, params: {progress: '@progress'}, outputs: []} # implicit: metrics, turn_index, counters_next, optimizers_next, rules_next, record
  after: # reads: models_final, optimizers_final, emas_final, counters_final, rules_final, record, composites, test_loader, prep, device, history, valid_loader; exports: predictions
    final: {uri: /lego/kalfa/save_final, inputs: {models: models_final, optimizers: optimizers_final, emas: emas_final, counters: counters_final, rules: 
          rules_final}, outputs: []}  # implicit: record
    report: {uri: /lego/kalfa/select, params: {which: last}, inputs: {models: models_final, emas: emas_final}, outputs: [selected]} # implicit: record
    predict: {uri: /lego/kalfa/predict, params: {predicts: simclr, set: test}, inputs: {models: selected, composites: composites, loader: test_loader, prep: 
          prep}, outputs: [predictions]} # implicit: record, device
    plots: {uri: /lego/kalfa/run_all, params: {plots: '@plots', keys: {loss_curve: {}}, predicts: simclr}, inputs: {predictions: predictions, history: history, 
          models: selected}, outputs: []} # implicit: record, composites, valid_loader, test_loader
    generate: {uri: /lego/kalfa/generate, params: {generate: null}, inputs: {models: selected, composites: composites, prep: prep}, outputs: []} # implicit: record
