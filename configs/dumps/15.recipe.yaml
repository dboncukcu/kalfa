# Generated by kalfa 0.2.2 from configs/15_multi_target.yaml: the driver document kalfa.driver.recipe produces,
# the one the templates of src/kalfa/templates/kalfa.yaml open. Not written by hand; regenerate with:
#   uv run python tests/fixtures/regenerate_recipes.py
# cirak's fixture copy replaces the include with kalfa.yaml and regenerates configs/dumps/*.flow.yaml.
include: [../../src/kalfa/templates/kalfa.yaml]
losses:
  l_y: {uri: /adapter/kalfa/criterion, params: {criterion: {uri: /criterion/kalfa/mse}}}
  l_z: {uri: /adapter/kalfa/criterion, params: {criterion: {uri: /criterion/kalfa/mse}}}
  l_tail: {uri: /adapter/kalfa/criterion, params: {criterion: {uri: /criterion/kalfa/bce_logits, params: {pos_weight: 
            5.0}}}}
  total: {uri: /objective/kalfa/weighted_sum, params: {terms: {l_y: 1.0, l_z: 1.0, l_tail: 0.5}}}
metrics:
  rmse_y: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/kalfa/rmse}}}
  rmse_z: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/kalfa/rmse}}}
  auroc_tail: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/torchmetrics/binary_auroc}}}
triggers:
  stop_0: {uri: /trigger/kalfa/plateau, params: {monitor: val/rmse_z, patience: 8}}
plots:
  loss_curve: {uri: /plot/kalfa/loss_curve}
  pred_vs_true: {uri: /plot/kalfa/pred_vs_true}
progress: {uri: /lego/kalfa/progress}
blocks:
  head:
    inputs: [x]
    outputs: [y_hat]
    spec:
    - {uri: /layer/kalfa/linear_relu, params: {out_features: 64}}
    - {uri: /layer/kalfa/linear, params: {out_features: 3}}
  combine:
    inputs: [y_hat]
    outputs: [z_hat]
    spec:
    - {uri: /layer/kalfa/linear_relu, params: {out_features: 32}}
    - {uri: /layer/kalfa/linear, params: {out_features: 1}}
  tail:
    inputs: [x]
    outputs: [tail_logit]
    spec:
    - {uri: /layer/kalfa/linear_relu, params: {out_features: 32}}
    - {uri: /layer/kalfa/linear, params: {out_features: 1}}
  full:
    inputs: [x]
    outputs: [y_hat, z_hat, tail_logit]
    graph:
      y_hat: {model: head, inputs: [x]}
      z_hat: {model: combine, inputs: [y_hat]}
      tail_logit: {model: tail, inputs: [x]}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/parquet, params: {path: scores.parquet}}, filter_pre: [], 
        filter_set: [], split: {uri: /split/kalfa/random, params: {ratios: [0.7, 0.15, 0.15], seed: 7}}, batch: {size: 
          256}, preprocessors: {x_scaler: {uri: /pre/sklearn/standard_scaler}, y_scaler: {uri: 
            /pre/sklearn/standard_scaler}, to_float: {uri: /pre/kalfa/cast, params: {dtype: float32}}}, 
        preprocessors_keys: {x_scaler: {}, y_scaler: {}, to_float: {}}, drop: [], fields: {y_*: {target: true, 
            preprocessors: [y_scaler]}, z: {target: true, preprocessors: [y_scaler]}, z_tail: {target: true, 
            preprocessors: [to_float]}, x*: {preprocessors: [x_scaler]}}, feed: {uri: /feed/kalfa/table, params: {}}}}
  models: {block: models, params: {trained_items: [{name: head, index: 0, init: null, trainable: true, weights: null}, {
            name: combine, index: 1, init: null, trainable: true, weights: null}, {name: tail, index: 2, init: null, 
            trainable: true, weights: null}], composite_items: [name: full], ema_items: [], trained_refs: {head: head, 
          combine: combine, tail: tail}, composite_refs: {full: full}, ema_refs: {}, seed: 7}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: main, uri: /optimizer/torch/adam, params: {lr: 0.001},
          loss: total, schedule: null, models: {head: head, combine: combine, tail: tail}}], optimizer_refs: {main: 
          opt_main}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {}}, turn_params: {}, losses_keys: {
          l_y: {output: y_hat, target: y_*}, l_z: {output: z_hat, target: z}, l_tail: {output: tail_logit, target: 
            z_tail}, total: {}}, metrics_keys: {rmse_y: {output: y_hat, target: y_*}, rmse_z: {output: z_hat, target: z},
        auroc_tail: {output: tail_logit, target: z_tail}}, predicts: full, epochs: 30, steps: null, rules: [], stop: ['@triggers.stop_0'],
      checkpoint: {uri: /checkpoint/kalfa/best, params: {monitor: val/rmse_z}}}}
  after: {block: after, params: {report: best, predicts: full, targets: {y_hat: y_*, z_hat: z, tail_logit: z_tail}, 
        generate: null, plots_keys: {loss_curve: {}, pred_vs_true: {}}}}
