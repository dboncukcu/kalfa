# Generated by kalfa 0.2.4 from configs/04_cnn_images.yaml: the driver document kalfa.driver.recipe produces,
# the one the templates of src/kalfa/templates/kalfa.yaml open. Not written by hand; regenerate with:
#   uv run python tests/fixtures/regenerate_recipes.py
# cirak's fixture copy replaces the include with kalfa.yaml and regenerates configs/dumps/*.flow.yaml.
include: [../../src/kalfa/templates/kalfa.yaml]
losses:
  ce: {uri: /adapter/kalfa/criterion, params: {criterion: {uri: /criterion/kalfa/cross_entropy}}}
metrics:
  accuracy: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/torchmetrics/accuracy}}}
triggers:
  unfreeze: {uri: /trigger/kalfa/after_turn, params: {at: 5}}
  stop_0: {uri: /trigger/kalfa/plateau, params: {monitor: val/accuracy, mode: max, patience: 4}}
plots:
  loss_curve: {uri: /plot/kalfa/loss_curve}
  confusion: {uri: /plot/kalfa/confusion_matrix}
progress: {uri: /lego/kalfa/progress}
blocks:
  backbone:
    inputs: [image]
    outputs: [feature]
    spec:
    - {uri: /layer/timm/timm_backbone, params: {name: resnet50, pretrained: true, pooled: true}}
  head:
    inputs: [feature]
    outputs: [logits]
    spec:
    - {uri: /layer/kalfa/linear, params: {out_features: 37}}
  classifier:
    inputs: [image]
    outputs: [logits]
    graph:
      feature: {model: backbone, inputs: [image]}
      logits: {model: head, inputs: [feature]}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/image_folder, params: {path: data/pets}}, filter_pre: [], 
        filter_set: [], split: {uri: /split/kalfa/random, params: {ratios: [0.9, 0.1, 0.0], seed: 7}}, batch: {size: 64, 
          eval_size: 256, balanced: true}, preprocessors: {resize: {uri: /pre/kalfa/resize, params: {size: 224}}, 
          to_tensor: {uri: /pre/kalfa/to_tensor}, augment: {uri: /pre/kalfa/random_crop_flip, params: {size: 224}}, 
          normalize: {uri: /pre/kalfa/normalize, params: {mean: imagenet, std: imagenet}}}, preprocessors_keys: {resize: {},
        to_tensor: {}, augment: {sets: [train]}, normalize: {}}, drop: [], fields: {image: {preprocessors: [resize, 
              augment, to_tensor, normalize]}, label: {target: true}}, feed: {uri: /feed/kalfa/table, params: {}}}}
  models: {block: models, params: {trained_items: [{name: backbone, index: 0, init: null, trainable: false, weights: 
            null}, {name: head, index: 1, init: null, trainable: true, weights: null}], composite_items: [name: 
            classifier], ema_items: [], trained_refs: {backbone: backbone, head: head}, composite_refs: {classifier: 
          classifier}, ema_refs: {}, seed: 7}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: main, uri: /optimizer/torch/adamw, params: {lr: 
              0.001, groups: [{match: backbone.*, lr: 1e-05}]}, loss: ce, schedule: null, models: {backbone: backbone, 
              head: head}}], optimizer_refs: {main: opt_main}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {}}, turn_params: {}, losses_keys: {
          ce: {}}, metrics_keys: {accuracy: {}}, predicts: classifier, epochs: 20, steps: null, rules: [{name: unfreeze, 
            when: '@triggers.unfreeze', set: {backbone.trainable: true}, after: null}], stop: ['@triggers.stop_0'], 
        checkpoint: {uri: /checkpoint/kalfa/best, params: {monitor: val/accuracy, mode: max}}}}
  after: {block: after, params: {report: best, figures: null, predicts: classifier, targets: {}, generate: null, 
        plots_keys: {loss_curve: {}, confusion: {}}}}
