# Generated by kalfa 0.2.2 from configs/13_distillation.yaml: the driver document kalfa.driver.recipe produces,
# the one the templates of src/kalfa/templates/kalfa.yaml open. Not written by hand; regenerate with:
#   uv run python tests/fixtures/regenerate_recipes.py
# cirak's fixture copy replaces the include with kalfa.yaml and regenerates configs/dumps/*.flow.yaml.
include: [../../src/kalfa/templates/kalfa.yaml]
losses:
  kd: {uri: /objective/kalfa/distill, params: {student: student, teacher: teacher, temperature: 4.0, alpha: 0.7}}
metrics:
  accuracy: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/torchmetrics/accuracy}}}
triggers:
  cool_down: {uri: /trigger/kalfa/metric_above, params: {monitor: val/accuracy, value: 0.9}}
  stop_0: {uri: /trigger/kalfa/plateau, params: {monitor: val/accuracy, mode: max, patience: 6}}
plots:
  loss_curve: {uri: /plot/kalfa/loss_curve, params: {series: [train/kd/ce, train/kd/kl, val/accuracy]}}
  confusion: {uri: /plot/kalfa/confusion_matrix}
progress: {uri: /lego/kalfa/progress}
blocks:
  teacher:
    inputs: [image]
    outputs: [logits]
    spec:
    - {uri: /layer/timm/timm_backbone, params: {name: resnet50, pooled: true}}
    - {uri: /layer/kalfa/linear, params: {out_features: 10}}
  student:
    inputs: [image]
    outputs: [logits]
    spec:
    - {uri: /layer/timm/timm_backbone, params: {name: resnet18, pooled: true}}
    - {uri: /layer/kalfa/linear, params: {out_features: 10}}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/image_folder, params: {path: data/cifar10}}, filter_pre: [], 
        filter_set: [], split: {uri: /split/kalfa/random, params: {ratios: [0.9, 0.1, 0.0], seed: 7}}, batch: {size: 128,
        eval_size: 512}, preprocessors: {to_tensor: {uri: /pre/kalfa/to_tensor}, normalize: {uri: /pre/kalfa/normalize, 
            params: {mean: cifar10, std: cifar10}}}, preprocessors_keys: {to_tensor: {}, normalize: {}}, drop: [], 
        fields: {image: {preprocessors: [to_tensor, normalize]}, label: {target: true}}, feed: {uri: /feed/kalfa/table, 
          params: {}}}}
  models: {block: models, params: {trained_items: [{name: teacher, index: 0, init: null, trainable: false, weights: {run: 
              runs/cifar_resnet50_x, model: net, which: best}}, {name: student, index: 1, init: null, trainable: true, 
            weights: null}], composite_items: [], ema_items: [], trained_refs: {teacher: teacher, student: student}, 
        composite_refs: {}, ema_refs: {}, seed: 7}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: main, uri: /optimizer/torch/adamw, params: {lr: 
              0.001}, loss: kd, schedule: null, models: {student: student}}], optimizer_refs: {main: opt_main}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {}}, turn_params: {}, losses_keys: {
          kd: {}}, metrics_keys: {accuracy: {}}, predicts: student, epochs: 40, steps: null, rules: [{name: cool_down, 
            when: '@triggers.cool_down', set: {main.lr: 0.0001}, after: null}], stop: ['@triggers.stop_0'], checkpoint: {
        uri: /checkpoint/kalfa/best, params: {monitor: val/accuracy, mode: max}}}}
  after: {block: after, params: {report: best, predicts: student, targets: {}, generate: null, plots_keys: {loss_curve: {},
        confusion: {}}}}
