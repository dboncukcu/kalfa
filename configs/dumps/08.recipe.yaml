# Generated by kalfa 0.2.0 from configs/08_ddpm.yaml: the driver document kalfa.driver.recipe produces,
# the one the templates of src/kalfa/templates/kalfa.yaml open. Not written by hand; regenerate with:
#   uv run python tests/fixtures/regenerate_recipes.py
# cirak's fixture copy replaces the include with kalfa.yaml and regenerates configs/dumps/*.flow.yaml.
include: [../../src/kalfa/templates/kalfa.yaml]
losses:
  ddpm: {uri: /objective/kalfa/ddpm, params: {model: unet, schedule: {uri: /schedule/kalfa/linear_betas, params: {steps: 
            1000}}}}
metrics:
  samples: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/kalfa/sample_writer, params: {sampler: {uri: 
              /generate/kalfa/ddpm_sampler, params: {model: unet.ema, schedule: {uri: /schedule/kalfa/linear_betas, 
                  params: {steps: 1000}}, shape: [3, 32, 32], n: 64}}, n: 16}}}}
triggers: {}
plots:
  loss_curve: {uri: /plot/kalfa/loss_curve}
progress: {uri: /lego/kalfa/progress}
blocks:
  unet:
    inputs: [image, t]
    outputs: [noise]
    graph:
      noise: {uri: /layer/unet/unet, params: {channels: 64}, inputs: [image, t]}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/image_folder, params: {path: data/cifar10}}, filter_pre: [], 
        filter_set: [], split: {uri: /split/kalfa/random, params: {ratios: [1.0, 0.0, 0.0], seed: 7}}, batch: {size: 128},
      preprocessors: {to_signed: {uri: /pre/kalfa/to_tensor_signed}}, preprocessors_keys: {to_signed: {}}, drop: [], 
        fields: {image: {preprocessors: [to_signed]}}, feed: {uri: /feed/kalfa/table, params: {}}}}
  models: {block: models, params: {trained_items: [{name: unet, index: 0, init: null, trainable: true, weights: null}], 
        composite_items: [], ema_items: [{name: unet, decay: 0.9999}], trained_refs: {unet: unet}, composite_refs: {}, 
        ema_refs: {unet: unet_ema}, seed: 7}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: main, uri: /optimizer/torch/adamw, params: {lr: 
              0.0002}, loss: ddpm, schedule: {uri: /schedule/kalfa/warmup_cosine, params: {warmup: 1000, total: 400000}},
          models: {unet: unet}}], optimizer_refs: {main: opt_main}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {}}, turn_params: {amp: true, 
          grad_clip: 1.0}, losses_keys: {ddpm: {}}, metrics_keys: {samples: {sets: [train], every: 5}}, predicts: unet, 
        epochs: 200, steps: null, rules: [], stop: [], checkpoint: {uri: /checkpoint/kalfa/last}}}
  after: {block: after, params: {report: last, predicts: unet, generate: {uri: /generate/kalfa/ddpm_sampler, params: {
            model: unet.ema, schedule: {uri: /schedule/kalfa/linear_betas, params: {steps: 1000}}, shape: [3, 32, 32], n: 
            64}}, plots_keys: {loss_curve: {}}}}
