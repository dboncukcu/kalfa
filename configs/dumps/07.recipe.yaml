# Generated by kalfa 0.2.2 from configs/07_wgan_gp.yaml: the driver document kalfa.driver.recipe produces,
# the one the templates of src/kalfa/templates/kalfa.yaml open. Not written by hand; regenerate with:
#   uv run python tests/fixtures/regenerate_recipes.py
# cirak's fixture copy replaces the include with kalfa.yaml and regenerates configs/dumps/*.flow.yaml.
include: [../../src/kalfa/templates/kalfa.yaml]
losses:
  wgan_d: {uri: /objective/kalfa/wgan_gp_d, params: {generator: generator, critic: critic, latent: 128, gp_weight: 10, 
        conditional: true}}
  wgan_g: {uri: /objective/kalfa/wgan_g, params: {generator: generator, critic: critic, latent: 128, conditional: true}}
metrics:
  fid: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/kalfa/fid, params: {model: generator.ema, latent: 128,
          conditional: true, n: 2000}}}}
  samples: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/kalfa/sample_writer, params: {sampler: {uri: 
              /generate/kalfa/gan_sampler, params: {model: generator.ema, latent: 128, conditional: true, n_classes: 10, 
                n: 64}}, n: 16}}}}
triggers: {}
plots:
  loss_curve: {uri: /plot/kalfa/loss_curve, params: {series: [train/wgan_d, train/wgan_g, val/fid]}}
  samples_gif: {uri: /plot/kalfa/samples_gif}
  samples_matrix: {uri: /plot/kalfa/samples_matrix, params: {n: 8}}
progress: {uri: /lego/kalfa/progress}
blocks:
  cond_embed:
    variables:
      n_classes:
        required: true
    spec:
    - {uri: /layer/torch/embedding, params: {num: $n_classes$, dim: 64}}
  generator:
    inputs: [z, label]
    outputs: [image]
    graph:
      y: {block: cond_embed, params: {n_classes: 10}, inputs: [label]}
      zy: {uri: /layer/torch/concat, params: {dim: 1}, inputs: [z, y]}
      image: {uri: /layer/dcgan/generator, params: {in_dim: 192, channels: 64}, inputs: [zy]}
  critic:
    inputs: [image, label]
    outputs: [score]
    graph:
      y: {block: cond_embed, params: {n_classes: 10}, inputs: [label]}
      score: {uri: /layer/dcgan/critic, params: {channels: 64, cond_dim: 64}, inputs: [image, y]}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/image_folder, params: {path: data/celeba64}}, filter_pre: [], 
        filter_set: [], split: {uri: /split/kalfa/random, params: {ratios: [0.95, 0.05, 0.0], seed: 7}}, batch: {size: 
          64, drop_last: true}, preprocessors: {to_signed: {uri: /pre/kalfa/to_tensor_signed}}, preprocessors_keys: {
          to_signed: {}}, drop: [], fields: {image: {preprocessors: [to_signed]}, label: {target: true}}, feed: {uri: 
          /feed/kalfa/table, params: {}}}}
  models: {block: models, params: {trained_items: [{name: generator, index: 0, init: {weights: {uri: /init/torch/normal, 
                params: {std: 0.02}}}, trainable: true, weights: null}, {name: critic, index: 1, init: {weights: {uri: 
                /init/torch/normal, params: {std: 0.02}}}, trainable: true, weights: null}], composite_items: [], 
        ema_items: [{name: generator, decay: 0.999}], trained_refs: {generator: generator, critic: critic}, 
        composite_refs: {}, ema_refs: {generator: generator_ema}, seed: 7}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: d, uri: /optimizer/torch/adam, params: {lr: 0.0001, 
              betas: [0.0, 0.9]}, loss: wgan_d, schedule: null, models: {critic: critic}}, {name: g, uri: 
            /optimizer/torch/adam, params: {lr: 0.0001, betas: [0.0, 0.9]}, loss: wgan_g, schedule: null, models: {
              generator: generator}}], optimizer_refs: {d: opt_d, g: opt_g}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {order: [d, g], steps: {d: 5, g: 1}, 
            fresh_batch: true}}, turn_params: {}, losses_keys: {wgan_d: {sets: [train]}, wgan_g: {}}, metrics_keys: {fid: {
          sets: [valid], every: 5}, samples: {sets: [valid], every: 5}}, predicts: null, epochs: 100, steps: null, rules: [],
      stop: [], checkpoint: {uri: /checkpoint/kalfa/best, params: {monitor: val/fid}}}}
  after: {block: after, params: {report: best, predicts: null, targets: {}, generate: {uri: /generate/kalfa/gan_sampler, 
          params: {model: generator.ema, latent: 128, conditional: true, n_classes: 10, n: 64}}, plots_keys: {loss_curve: {},
        samples_gif: {}, samples_matrix: {}}}}
