# Generated by kalfa 0.2.2 from configs/02_mlp_classification.yaml: the driver document kalfa.driver.recipe produces,
# the one the templates of src/kalfa/templates/kalfa.yaml open. Not written by hand; regenerate with:
#   uv run python tests/fixtures/regenerate_recipes.py
# cirak's fixture copy replaces the include with kalfa.yaml and regenerates configs/dumps/*.flow.yaml.
include: [../../src/kalfa/templates/kalfa.yaml]
losses:
  ce: {uri: /adapter/kalfa/criterion, params: {criterion: {uri: /criterion/kalfa/cross_entropy, params: {weight: {uri: 
              /data/kalfa/class_weights}}}}}
metrics:
  accuracy: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/torchmetrics/accuracy}}}
  f1: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/torchmetrics/f1}}}
triggers:
  stop_0: {uri: /trigger/kalfa/plateau, params: {monitor: val/f1, mode: max, patience: 5}}
plots:
  loss_curve: {uri: /plot/kalfa/loss_curve}
  confusion: {uri: /plot/kalfa/confusion_matrix}
progress: {uri: /lego/kalfa/progress}
blocks:
  net:
    inputs: [x]
    outputs: [logits]
    spec:
    - {uri: /layer/kalfa/linear_relu, params: {out_features: 64}}
    - {uri: /layer/kalfa/linear_relu, params: {out_features: 64}}
    - {uri: /layer/kalfa/linear, params: {out_features: 2}}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/parquet, params: {path: churn.parquet}}, filter_pre: [], 
        filter_set: [], split: {uri: /split/kalfa/random, params: {ratios: [0.7, 0.15, 0.15], seed: 7}}, batch: {size: 
          256}, preprocessors: {std_scaler: {uri: /pre/sklearn/standard_scaler}, onehot: {uri: /pre/kalfa/one_hot}, 
          label: {uri: /pre/kalfa/label_encoder}}, preprocessors_keys: {std_scaler: {}, onehot: {}, label: {}}, drop: [],
      fields: {num_*: {preprocessors: [std_scaler]}, cat_*: {preprocessors: [onehot]}, churned: {target: true, 
            preprocessors: [label]}}, feed: {uri: /feed/kalfa/table, params: {}}}}
  models: {block: models, params: {trained_items: [{name: net, index: 0, init: null, trainable: true, weights: null}], 
        composite_items: [], ema_items: [], trained_refs: {net: net}, composite_refs: {}, ema_refs: {}, seed: 7}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: net, uri: /optimizer/torch/adam, params: {lr: 0.001},
          loss: ce, schedule: null, models: {net: net}}], optimizer_refs: {net: opt_net}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {}}, turn_params: {}, losses_keys: {
          ce: {}}, metrics_keys: {accuracy: {}, f1: {}}, predicts: net, epochs: 30, steps: null, rules: [], stop: ['@triggers.stop_0'],
      checkpoint: {uri: /checkpoint/kalfa/best, params: {monitor: val/f1, mode: max}}}}
  after: {block: after, params: {report: best, predicts: net, targets: {}, generate: null, plots_keys: {loss_curve: {}, 
          confusion: {}}}}
