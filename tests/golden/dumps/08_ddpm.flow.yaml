# Generated by kalfa 0.2.4 (cirak 0.2.3) from examples/08_ddpm/config.yaml: kalfa check --dump, the flow the
# contract of src/kalfa/contract.yaml opens for the driver document tests/golden/recipes/08_ddpm.recipe.yaml.
# Regenerate with: uv run python tools/regenerate.py dumps
# Sections: components (the recipe's component tables), blocks (spec and graph blocks as templates;
# the flow blocks are opened inside flow), flow (the expanded flow with every output written out;
# the comments are tezgah's resolution: implicit bindings, pattern matches, derived reads and exports).
components:
  losses:
    ddpm: {uri: /adapter/kalfa/objective, params: {objective: {uri: /objective/kalfa/ddpm, params: {model: unet, schedule: {uri: /schedule/kalfa/linear_betas, 
                params: {steps: 1000}}}}}}
  metrics:
    samples: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/kalfa/sample_writer, params: {sampler: {uri: /generate/kalfa/ddpm_sampler, params: {
                  model: unet.ema, schedule: {uri: /schedule/kalfa/linear_betas, params: {steps: 1000}}, shape: [3, 32, 32], n: 64}}, n: 16}}}}
  triggers: {}
  plots:
    loss_curve: {uri: /plot/kalfa/loss_curve}
  checkpoint: {uri: /checkpoint/kalfa/last}
blocks:
  unet:
    inputs: [image, t]
    outputs: [noise]
    graph:
      noise: {uri: /layer/unet/unet, params: {channels: 64}, inputs: [image, t]}
flow:
  outputs: [history, predictions]
  data:  # reads: record; exports: prep, train_loader, valid_loader, test_loader
    source: {uri: /source/kalfa/image_folder, params: {path: data/cifar10}, outputs: [df_0]}
    split: {uri: /split/kalfa/random, params: {ratios: [1.0, 0.0, 0.0], seed: 7}, inputs: {df: df_0}, outputs: {train: train_df_0, valid: valid_df_0, test: 
          test_df_0}}
    set_transform_train: {uri: /lego/kalfa/transform_set, params: {set: train, transforms: []}, inputs: {df: train_df_0}, outputs: [train_df_1]}
    set_transform_valid: {uri: /lego/kalfa/transform_set, params: {set: valid, transforms: []}, inputs: {df: valid_df_0}, outputs: [valid_df_1]}
    set_transform_test: {uri: /lego/kalfa/transform_set, params: {set: test, transforms: []}, inputs: {df: test_df_0}, outputs: [test_df_1]}
    frames: {uri: /lego/kalfa/fit_frames, params: {frames: []}, inputs: {df: train_df_1}, outputs: [frames]}  # implicit: record
    framed_train: {uri: /lego/kalfa/apply_frames, inputs: {df: train_df_1, frames: frames}, outputs: [train_df]}
    framed_valid: {uri: /lego/kalfa/apply_frames, inputs: {df: valid_df_1, frames: frames}, outputs: [valid_df]}
    framed_test: {uri: /lego/kalfa/apply_frames, inputs: {df: test_df_1, frames: frames}, outputs: [test_df]}
    prep: {uri: /lego/kalfa/fit, params: {fields: {image: {preprocessors: [to_signed]}}, preprocessors: {to_signed: {uri: /pre/kalfa/to_tensor_signed}}, drop: [],
        keys: {to_signed: {}}}, inputs: {df: train_df}, outputs: [prep]}  # implicit: record
    frame_train: {uri: /lego/kalfa/apply, params: {set: train, keys: {to_signed: {}}}, inputs: {df: train_df, prep: prep}, outputs: [train_frame]}
    frame_valid: {uri: /lego/kalfa/apply, params: {set: valid, keys: {to_signed: {}}}, inputs: {df: valid_df, prep: prep}, outputs: [valid_frame]}
    frame_test: {uri: /lego/kalfa/apply, params: {set: test, keys: {to_signed: {}}}, inputs: {df: test_df, prep: prep}, outputs: [test_frame]}
    feed_train: {uri: /feed/kalfa/table, inputs: {frame: train_frame, frames: {train: train_frame, valid: valid_frame, test: test_frame}}, outputs: [train_data]}
    feed_valid: {uri: /feed/kalfa/table, inputs: {frame: valid_frame, frames: {train: train_frame, valid: valid_frame, test: test_frame}}, outputs: [valid_data]}
    feed_test: {uri: /feed/kalfa/table, inputs: {frame: test_frame, frames: {train: train_frame, valid: valid_frame, test: test_frame}}, outputs: [test_data]}
    loader_train: {uri: /loader/kalfa/torch, params: {set: train, size: 128}, inputs: {data: train_data}, outputs: [train_loader]}
    loader_valid: {uri: /loader/kalfa/torch, params: {set: valid, size: 128}, inputs: {data: valid_data}, outputs: [valid_loader]}
    loader_test: {uri: /loader/kalfa/torch, params: {set: test, size: 128}, inputs: {data: test_data}, outputs: [test_loader]}
  models: # reads: prep, train_loader; exports: unet, models, emas, composites
    build_unet: {block: unet, builder: /builder/kalfa/module, params: {rng: {uri: /rng/kalfa/derived, params: {}}, seed: 7, name: unet, index: 0, init: null, 
          trainable: true, weights: null}, outputs: [unet]}  # implicit: prep, train_loader
    ema_unet: {uri: /lego/kalfa/clone, params: {decay: 0.9999}, inputs: {model: unet}, outputs: [unet_ema]}
    models: {uri: /lego/kalfa/pack, inputs: {items: {unet: unet}}, outputs: [models]}
    emas: {uri: /lego/kalfa/pack, inputs: {items: {unet: unet_ema}}, outputs: [emas]}
    composites: {uri: /lego/kalfa/pack, inputs: {items: {}}, outputs: [composites]}
  optimizers: # reads: unet; exports: optimizers
    build_opt_main: {uri: /optimizer/torch/adamw, params: {params: {lr: 0.0002}, schedule: {uri: /schedule/kalfa/warmup_cosine, params: {warmup: 1000, total: 
              400000}}, loss: ddpm}, inputs: {models: {unet: unet}}, outputs: [opt_main]}
    optimizers: {uri: /lego/kalfa/pack, inputs: {items: {main: opt_main}}, outputs: [optimizers]}
  training: # reads: models, optimizers, emas, device, composites, train_loader, prep, record, valid_loader, test_loader, monitor; exports: history, models_final, optimizers_final, emas_final, counters_final, rules_final
    counters: {uri: /lego/kalfa/const, params: {value: {global_step: 0, turn: 0}}, outputs: [counters]}
    rules: {uri: /lego/kalfa/const, params: {value: {}}, outputs: [rules]}
    stream: {uri: /lego/kalfa/const, params: {value: null}, outputs: [stream]}
    init: {uri: /lego/kalfa/init_state, params: {epochs: 200, steps: null, policy: '@checkpoint'}, inputs: {state: {models: models, optimizers: optimizers, emas: 
            emas, counters: counters, rules: rules}}, outputs: [epochs_left]}  # implicit: device
    epochs: # reads: composites, train_loader, device, prep, record, valid_loader, test_loader, monitor
      loop:
        carry: [models, optimizers, emas, counters, rules, stream]
        next: _next
        range: epochs_left
        index: turn_index
        until: stop
        trace:
          metrics: history
        outputs:
          models: models_final
          optimizers: optimizers_final
          emas: emas_final
          counters: counters_final
          rules: rules_final
        body:  # reads: rules, models, optimizers, emas, counters, composites, train_loader, stream, device, prep, record, valid_loader, test_loader, monitor, turn_index; exports: models_next, optimizers_next, emas_next, counters_next, stream_next, metrics, rules_next, stop
          effects: {uri: /rule/kalfa/effects, inputs: {rules: rules}, outputs: [effects]}
          turn: {uri: /turn/kalfa/alternating, params: {params: {}, extra: {amp: true, grad_clip: 1.0}, losses: '@losses', metrics: '@metrics', losses_keys: {
                  ddpm: {}}, metrics_keys: {samples: {sets: [train], every: 5}}, predicts: unet, steps: null}, inputs: {models: models, optimizers: optimizers, 
                emas: emas, counters: counters, composites: composites, effects: effects, loader: train_loader, stream: stream}, outputs: {models: models_next, 
                optimizers: optimizers_next, emas: emas_next, counters: counters_next, stream: stream_next, metrics: train_metrics}}  # implicit: device, prep, record
          evaluate_valid: {uri: /lego/kalfa/evaluate, params: {set: valid, losses: '@losses', metrics: '@metrics', losses_keys: {ddpm: {}}, metrics_keys: {
                  samples: {sets: [train], every: 5}}, predicts: unet}, inputs: {models: models_next, emas: emas_next, composites: composites, counters: 
                counters_next, effects: effects, loader: valid_loader}, outputs: [valid_metrics]} # implicit: device, prep, record
          evaluate_test: {uri: /lego/kalfa/evaluate, params: {set: test, losses: '@losses', metrics: '@metrics', losses_keys: {ddpm: {}}, metrics_keys: {samples: {
                  sets: [train], every: 5}}, predicts: unet}, inputs: {models: models_next, emas: emas_next, composites: composites, counters: counters_next, 
                effects: effects, loader: test_loader}, outputs: [test_metrics]} # implicit: device, prep, record
          metrics: {uri: /lego/kalfa/merge, params: {prefixes: {train: train, valid: val, test: test}}, inputs: {parts: '*_metrics'}, outputs: [metrics]} # parts *_metrics: test_metrics, train_metrics, valid_metrics
          rules_0: {uri: /rule/kalfa/open, inputs: {rules: rules}, outputs: [rules_0]}
          rules_ruled: {uri: /lego/kalfa/identity, inputs: {value: rules_0}, outputs: [rules_ruled]}
          stop: {uri: /rule/kalfa/stop, params: {triggers: []}, inputs: {rules: rules_ruled}, outputs: {rules: rules_next, stop: stop}}  # implicit: metrics
          checkpoint: {uri: /lego/kalfa/checkpoint, params: {policy: '@checkpoint'}, inputs: {state: '*_next'}, outputs: []} # state *_next: counters_next, emas_next, models_next, optimizers_next, rules_next, stream_next; implicit: metrics, record
          log: {uri: /lego/kalfa/history, outputs: []} # implicit: monitor, metrics, turn_index, counters_next, optimizers_next, rules_next, record
  after: # reads: models_final, optimizers_final, emas_final, counters_final, rules_final, record, composites, test_loader, prep, device, history, train_loader, valid_loader; exports: predictions
    final: {uri: /lego/kalfa/save_final, inputs: {models: models_final, optimizers: optimizers_final, emas: emas_final, counters: counters_final, rules: 
          rules_final}, outputs: []}  # implicit: record
    report: {uri: /lego/kalfa/select, params: {which: last}, inputs: {models: models_final, emas: emas_final}, outputs: [selected]} # implicit: record
    predict: {uri: /lego/kalfa/predict, params: {predicts: unet, set: test, target_map: {}}, inputs: {models: selected, composites: composites, loader: 
          test_loader, prep: prep}, outputs: [predictions]} # implicit: record, device
    figures: {uri: /lego/kalfa/figures, outputs: [figures]}
    losses: {uri: /lego/kalfa/identity, params: {value: '@losses'}, outputs: [losses]}
    losses_keys: {uri: /lego/kalfa/const, params: {value: {ddpm: {}}}, outputs: [losses_keys]}
    plots: {uri: /lego/kalfa/run_all, params: {plots: '@plots', keys: {loss_curve: {lego: /plot/kalfa/loss_curve}}, predicts: unet}, inputs: {predictions: 
          predictions, history: history, models: selected, figures: figures, bus: {prep: prep, train_loader: train_loader, valid_loader: valid_loader, 
            test_loader: test_loader, composites: composites, device: device, counters: counters_final, optimizers: optimizers_final, emas: emas_final, rules: 
            rules_final, losses: losses, losses_keys: losses_keys}}, outputs: []} # implicit: record
    generate: {uri: /lego/kalfa/generate, params: {generate: {uri: /generate/kalfa/ddpm_sampler, params: {model: unet.ema, schedule: {uri: 
                /schedule/kalfa/linear_betas, params: {steps: 1000}}, shape: [3, 32, 32], n: 64}}}, inputs: {models: selected, composites: composites, prep: 
          prep}, outputs: []} # implicit: record
