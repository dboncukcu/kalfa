# Generated by kalfa 0.2.4 from examples/alad/config.yaml: the driver document kalfa.driver.recipe produces,
# the one the blocks of src/kalfa/contract.yaml open. Not written by hand; regenerate with:
#   uv run python tools/regenerate.py recipes
include: [../../../src/kalfa/contract.yaml]
losses:
  adv_d: {uri: /adapter/kalfa/objective, params: {objective: {uri: /objective/myexample/alad_discriminator, params: {
            criterion: {uri: /criterion/kalfa/bce_logits}, latent_dim: 4}}}}
  adv_g: {uri: /adapter/kalfa/objective, params: {objective: {uri: /objective/myexample/alad_generator, params: {
            criterion: {uri: /criterion/kalfa/bce_logits}, latent_dim: 4, feature_matching: 0.0}}}}
metrics:
  auroc: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/torchmetrics/binary_auroc}}}
  average_precision: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/torchmetrics/binary_average_precision}}}
triggers: {}
plots:
  score_histogram: {uri: /plot/kalfa/class_histogram}
  roc: {uri: /plot/torchmetrics/binary_roc}
  pr_curve: {uri: /plot/torchmetrics/binary_precision_recall_curve}
  architecture: {uri: /plot/kalfa/architecture}
  modules: {uri: /plot/kalfa/architecture_text}
checkpoint: {}
blocks:
  mlp_hidden:
    variables:
      width:
        required: true
    spec:
    - {uri: /layer/torch/linear, params: {in_features: $width$, out_features: $width$}}
    - {uri: /layer/torch/relu}
  mlp:
    variables:
      in_features:
        required: true
      out_features:
        required: true
      width:
        default: 64
      depth:
        default: 2
    inputs: [x]
    outputs: [y]
    graph:
      h0: {uri: /layer/torch/linear, params: {in_features: $in_features$, out_features: $width$}, inputs: [x]}
      a0: {uri: /layer/torch/relu, inputs: [h0]}
      h: {block: mlp_hidden, params: {width: $width$}, inputs: [a0], repeat: $depth$}
      j: {uri: /layer/torch/concat, params: {dim: 1}, inputs: [x, h]}
      y: {uri: /layer/kalfa/linear, params: {out_features: $out_features$}, inputs: [j]}
  branch:
    variables:
      in_features:
        required: true
      width:
        required: true
    spec:
    - {uri: /layer/torch/linear, params: {in_features: $in_features$, out_features: $width$}}
    - {uri: /layer/torch/leaky_relu, params: {negative_slope: 0.2}}
  head:
    variables:
      in_features:
        required: true
      width:
        required: true
    inputs: [c]
    outputs: [feature, logit]
    graph:
      h: {uri: /layer/torch/linear, params: {in_features: $in_features$, out_features: $width$}, inputs: [c]}
      feature: {uri: /layer/torch/leaky_relu, params: {negative_slope: 0.2}, inputs: [h]}
      d: {uri: /layer/torch/dropout, params: {p: 0.2}, inputs: [feature]}
      logit: {uri: /layer/torch/linear, params: {in_features: $width$, out_features: 1}, inputs: [d]}
  encoder:
    inputs: [x]
    outputs: [z]
    spec:
    - {block: mlp, params: {in_features: 6, out_features: 64, width: 64, depth: 2}}
    - {uri: /layer/torch/leaky_relu, params: {negative_slope: 0.2}}
    - {uri: /layer/torch/linear, params: {in_features: 64, out_features: 4}}
  generator:
    inputs: [z]
    outputs: [x_hat]
    spec:
    - {uri: /layer/torch/linear, params: {in_features: 4, out_features: 64}}
    - {uri: /layer/torch/relu}
    - {uri: /layer/torch/linear, params: {in_features: 64, out_features: 64}}
    - {uri: /layer/torch/relu}
    - {uri: /layer/torch/linear, params: {in_features: 64, out_features: 6}}
  dxz:
    inputs: [x, z]
    outputs: [logit]
    graph:
      xb: {block: branch, params: {in_features: 6, width: 32}, inputs: [x]}
      zb: {block: branch, params: {in_features: 4, width: 32}, inputs: [z]}
      j: {uri: /layer/torch/concat, params: {dim: 1}, inputs: [xb, zb]}
      head: {block: head, params: {in_features: 64, width: 64}, inputs: [j], outputs: [feature, logit]}
  dxx:
    inputs: [x, x_hat]
    outputs: [logit, feature]
    graph:
      j: {uri: /layer/torch/concat, params: {dim: 1}, inputs: [x, x_hat]}
      head: {block: head, params: {in_features: 12, width: 64}, inputs: [j], outputs: [feature, logit]}
  dzz:
    inputs: [z, z_hat]
    outputs: [logit]
    graph:
      j: {uri: /layer/torch/concat, params: {dim: 1}, inputs: [z, z_hat]}
      head: {block: head, params: {in_features: 8, width: 32}, inputs: [j], outputs: [feature, logit]}
  anomaly_score:
    inputs: [x]
    outputs: [score]
    graph:
      z: {model: encoder, inputs: [x]}
      x_hat: {model: generator, inputs: [z]}
      rec: {model: dxx, inputs: [x, x_hat], outputs: [logit_rec, feature_rec]}
      ref: {model: dxx, inputs: [x, x], outputs: [logit_ref, feature_ref]}
      score: {uri: /layer/kalfa/l1_distance, inputs: [feature_rec, feature_ref]}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/parquet, params: {path: anomaly_data.parquet}}, transform_pre: [
        {uri: /transform/kalfa/filter, params: {query: (is_anomaly > -4) & (is_anomaly < 4)}}], set_transforms: {train: {
          set: train, transforms: [{uri: /transform/kalfa/filter, params: {query: is_anomaly == 0}}]}, valid: {set: 
            valid, transforms: []}, test: {set: test, transforms: []}}, split: {uri: /split/kalfa/random, params: {
            ratios: [0.7, 0.1, 0.2], seed: 42}}, loaders: {train: {uri: /loader/kalfa/torch, set: train, params: {set: 
              train, size: 50}}, valid: {uri: /loader/kalfa/torch, set: valid, params: {set: valid, size: 50}}, test: {
            uri: /loader/kalfa/torch, set: test, params: {set: test, size: 50}}}, frames: {uri: /lego/kalfa/fit_frames, 
          params: {frames: []}, inputs: {df: train_df_1}}, prep: {uri: /lego/kalfa/fit, params: {fields: {x?: {
                preprocessors: [abs_scaler, log_scaler, std_scaler]}, xx*: {preprocessors: [abs_scaler, log_scaler, 
                  std_scaler]}, is_anomaly: {target: true, preprocessors: [cast_int]}}, preprocessors: {abs_scaler: {uri: 
                /pre/kalfa/abs}, log_scaler: {uri: /pre/kalfa/log, params: {base: 10, norm: 1200}}, std_scaler: {uri: 
                /pre/sklearn/standard_scaler}, cast_int: {uri: /pre/kalfa/cast, params: {dtype: int}}}, drop: [xx6, xx7],
          keys: {abs_scaler: {}, log_scaler: {}, std_scaler: {}, cast_int: {}}}, inputs: {df: train_df}}, 
        preprocessors_keys: {abs_scaler: {}, log_scaler: {}, std_scaler: {}, cast_int: {}}, feed: {uri: 
          /feed/kalfa/table, params: {}}}}
  models: {block: models, params: {trained_items: [{name: encoder, index: 0, init: null, trainable: true, weights: null},
        {name: generator, index: 1, init: {weights: {uri: /init/torch/normal, params: {std: 0.02}}}, trainable: true, 
            weights: null}, {name: dxz, index: 2, init: {weights: {uri: /init/torch/normal, params: {std: 0.02}}}, 
            trainable: true, weights: null}, {name: dxx, index: 3, init: {weights: {uri: /init/torch/normal, params: {
                  std: 0.02}}}, trainable: true, weights: null}, {name: dzz, index: 4, init: {weights: {uri: 
                /init/torch/normal, params: {std: 0.02}}}, trainable: true, weights: null}], composite_items: [name: 
            anomaly_score], ema_items: [], trained_refs: {encoder: encoder, generator: generator, dxz: dxz, dxx: dxx, 
          dzz: dzz}, composite_refs: {anomaly_score: anomaly_score}, ema_refs: {}, seed: 42, rng: {uri: 
          /rng/kalfa/derived, params: {}}, builder: /builder/kalfa/module}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: g, uri: /optimizer/torch/adam, params: {lr: 0.0002, 
              betas: [0.5, 0.999]}, loss: adv_g, schedule: null, models: {encoder: encoder, generator: generator}}, {
            name: d, uri: /optimizer/torch/adam, params: {lr: 0.0002, betas: [0.5, 0.999]}, loss: adv_d, schedule: null, 
            models: {dxz: dxz, dxx: dxx, dzz: dzz}}], optimizer_refs: {g: opt_g, d: opt_d}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {order: [d, g], steps: {d: 1, g: 1}}},
      turn_params: {}, losses_keys: {adv_d: {}, adv_g: {}}, metrics_keys: {auroc: {}, average_precision: {}}, predicts: 
        anomaly_score, epochs: 60, steps: null, rules: [], stop: [], history_prefix: {train: train, valid: val, test: 
          test}}}
  after: {block: after, params: {report: last, figures: {uri: /lego/kalfa/figures, params: {}}, predicts: anomaly_score, 
        targets: {}, generate: null, plots_keys: {score_histogram: {lego: /plot/kalfa/class_histogram}, roc: {lego: 
            /plot/torchmetrics/binary_roc}, pr_curve: {lego: /plot/torchmetrics/binary_precision_recall_curve}, 
          architecture: {lego: /plot/kalfa/architecture}, modules: {lego: /plot/kalfa/architecture_text}}, plot_bus: {
          prep: prep, train_loader: train_loader, valid_loader: valid_loader, test_loader: test_loader, composites: 
          composites, device: device, counters: counters_final, optimizers: optimizers_final, emas: emas_final, rules: 
          rules_final, losses: losses, losses_keys: losses_keys, data_report: data_report, train_df: train_df, 
          train_frame: train_frame}, losses_keys: {adv_d: {}, adv_g: {}}}}
