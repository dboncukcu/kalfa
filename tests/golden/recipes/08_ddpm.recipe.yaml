# Generated by kalfa 0.2.4 from examples/08_ddpm/config.yaml: the driver document kalfa.driver.recipe produces,
# the one the blocks of src/kalfa/contract.yaml open. Not written by hand; regenerate with:
#   uv run python tools/regenerate.py recipes
include: [../../../src/kalfa/contract.yaml]
losses:
  ddpm: {uri: /adapter/kalfa/objective, params: {objective: {uri: /objective/kalfa/ddpm, params: {model: unet, schedule: {
            uri: /schedule/kalfa/linear_betas, params: {steps: 1000}}}}}}
metrics:
  samples: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/kalfa/sample_writer, params: {sampler: {uri: 
              /generate/kalfa/ddpm_sampler, params: {model: unet.ema, schedule: {uri: /schedule/kalfa/linear_betas, 
                  params: {steps: 1000}}, shape: [3, 32, 32], n: 64}}, n: 16}}}}
triggers: {}
plots:
  loss_curve: {uri: /plot/kalfa/loss_curve}
calibrate: {}
checkpoint: {uri: /checkpoint/kalfa/last}
blocks:
  unet:
    inputs: [image, t]
    outputs: [noise]
    graph:
      noise: {uri: /layer/unet/unet, params: {channels: 64}, inputs: [image, t]}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/image_folder, params: {path: data/cifar10}}, transform_pre: [],
      set_transforms: {train: {set: train, transforms: []}, valid: {set: valid, transforms: []}, test: {set: test, 
            transforms: []}}, split: {uri: /split/kalfa/random, params: {ratios: [1.0, 0.0, 0.0], seed: 7}}, loaders: {
          train: {uri: /loader/kalfa/torch, set: train, params: {set: train, size: 128}}, valid: {uri: 
            /loader/kalfa/torch, set: valid, params: {set: valid, size: 128}}, test: {uri: /loader/kalfa/torch, set: 
            test, params: {set: test, size: 128}}}, mask: null, frames: {uri: /lego/kalfa/fit_frames, params: {frames: []},
        inputs: {df: train_df_1}}, prep: {uri: /lego/kalfa/fit, params: {fields: {image: {preprocessors: [to_signed]}}, 
            preprocessors: {to_signed: {uri: /pre/kalfa/to_tensor_signed}}, drop: [], keys: {to_signed: {}}}, inputs: {
            df: train_df}}, preprocessors_keys: {to_signed: {}}, feed: {uri: /feed/kalfa/table, params: {}}}}
  models: {block: models, params: {trained_items: [{name: unet, index: 0, init: null, trainable: true, weights: null}], 
        composite_items: [], ema_items: [{name: unet, decay: 0.9999}], trained_refs: {unet: unet}, composite_refs: {}, 
        ema_refs: {unet: unet_ema}, seed: 7, rng: {uri: /rng/kalfa/derived, params: {}}, builder: /builder/kalfa/module}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: main, uri: /optimizer/torch/adamw, params: {lr: 
              0.0002}, loss: ddpm, schedule: {uri: /schedule/kalfa/warmup_cosine, params: {warmup: 1000, total: 400000}},
          models: {unet: unet}}], optimizer_refs: {main: opt_main}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {}}, turn_params: {amp: true, 
          grad_clip: 1.0}, losses_keys: {ddpm: {}}, metrics_keys: {samples: {sets: [train], every: 5}}, predicts: unet, 
        epochs: 200, steps: null, rules: [], stop: [], history_prefix: {train: train, valid: val, test: test}}}
  after: {block: after, params: {report: last, figures: {uri: /lego/kalfa/figures, params: {}}, predicts: unet, targets: {},
      generate: {uri: /generate/kalfa/ddpm_sampler, params: {model: unet.ema, schedule: {uri: 
              /schedule/kalfa/linear_betas, params: {steps: 1000}}, shape: [3, 32, 32], n: 64}}, plots_keys: {loss_curve: {
          lego: /plot/kalfa/loss_curve}}, plot_bus: {prep: prep, train_loader: train_loader, valid_loader: valid_loader, 
          test_loader: test_loader, composites: composites, device: device, counters: counters_final, optimizers: 
          optimizers_final, emas: emas_final, rules: rules_final, losses: losses, losses_keys: losses_keys, data_report: 
          data_report, train_df: train_df, train_frame: train_frame}, losses_keys: {ddpm: {}}}}
