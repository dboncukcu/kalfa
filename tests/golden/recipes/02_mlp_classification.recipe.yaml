# Generated by kalfa 0.2.6 from examples/02_mlp_classification/config.yaml: the driver document kalfa.driver.recipe produces,
# the one the blocks of src/kalfa/contract.yaml open. Not written by hand; regenerate with:
#   uv run python tools/regenerate.py recipes
include: [../../../src/kalfa/contract.yaml]
losses:
  ce: {uri: /adapter/kalfa/criterion, params: {criterion: {uri: /criterion/kalfa/cross_entropy, params: {weight: {uri: 
              /data/kalfa/class_weights}}}}}
metrics:
  accuracy: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/torchmetrics/accuracy}}}
  f1: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/torchmetrics/f1}}}
triggers:
  stop_0: {uri: /trigger/kalfa/plateau, params: {monitor: val/f1, mode: max, patience: 5}}
plots:
  loss_curve: {uri: /plot/kalfa/loss_curve}
  confusion: {uri: /plot/kalfa/confusion_matrix}
calibrate: {}
checkpoint: {uri: /checkpoint/kalfa/best, params: {monitor: val/f1, mode: max}}
blocks:
  net:
    inputs: [x]
    outputs: [logits]
    spec:
    - {uri: /layer/kalfa/linear_relu, params: {out_features: 64}}
    - {uri: /layer/kalfa/linear_relu, params: {out_features: 64}}
    - {uri: /layer/kalfa/linear, params: {out_features: 2}}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/parquet, params: {path: churn.parquet}}, sets: [train, valid, 
          test], transform_pre: [], set_transforms: {train: {set: train, transforms: []}, valid: {set: valid, transforms: []},
        test: {set: test, transforms: []}}, transform_notes: [], set_transform_notes: {}, split: {uri: 
          /split/kalfa/random, params: {ratios: [0.7, 0.15, 0.15], seed: 7}}, split_outputs: {train: train_df_0, valid: 
          valid_df_0, test: test_df_0}, frame_refs: {train: train_frame, valid: valid_frame, test: test_frame}, loaders: {
        train: {uri: /loader/kalfa/torch, set: train, params: {set: train, size: 256}}, valid: {uri: /loader/kalfa/torch,
          set: valid, params: {set: valid, size: 256}}, test: {uri: /loader/kalfa/torch, set: test, params: {set: test, 
              size: 256}}}, mask: null, frames: {uri: /lego/kalfa/fit_frames, params: {frames: []}, inputs: {df: 
            train_df_1}}, prep: {uri: /lego/kalfa/fit, params: {fields: {num_*: {preprocessors: [std_scaler]}, cat_*: {
                preprocessors: [onehot]}, churned: {target: true, preprocessors: [label]}}, preprocessors: {std_scaler: {
              uri: /pre/sklearn/standard_scaler}, onehot: {uri: /pre/kalfa/one_hot}, label: {uri: 
                /pre/kalfa/label_encoder}}, drop: [], keys: {std_scaler: {}, onehot: {}, label: {}}, spectators: []}, 
          inputs: {df: train_df}}, preprocessors_keys: {std_scaler: {}, onehot: {}, label: {}}, feed: {uri: 
          /feed/kalfa/table, params: {}}}}
  models: {block: models, params: {trained_items: [{name: net, index: 0, init: null, trainable: true, weights: null}], 
        composite_items: [], ema_items: [], trained_refs: {net: net}, composite_refs: {}, ema_refs: {}, seed: 7, rng: {
          uri: /rng/kalfa/derived, params: {}}, builder: /builder/kalfa/module}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: net, uri: /optimizer/torch/adam, params: {lr: 0.001},
          loss: ce, schedule: null, models: {net: net}}], optimizer_refs: {net: opt_net}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {}}, turn_params: {}, losses_keys: {
          ce: {}}, metrics_keys: {accuracy: {}, f1: {}}, predicts: net, epochs: 30, steps: null, rules: [], stop: ['@triggers.stop_0'],
      sets: [valid, test], history_prefix: {train: train, valid: val, test: test}}}
  after: {block: after, params: {report: best, figures: {uri: /lego/kalfa/figures, params: {}}, predicts: net, targets: {},
      generate: null, plots_keys: {loss_curve: {lego: /plot/kalfa/loss_curve}, confusion: {lego: 
            /plot/kalfa/confusion_matrix}}, plot_bus: {prep: prep, train_loader: train_loader, valid_loader: 
          valid_loader, test_loader: test_loader, composites: composites, device: device, counters: counters_final, 
          optimizers: optimizers_final, emas: emas_final, rules: rules_final, losses: losses, losses_keys: losses_keys, 
          data_report: data_report, train_df: train_df, train_frame: train_frame}, loader_refs: {train: train_loader, 
          valid: valid_loader, test: test_loader}, extra_sets: [], losses_keys: {ce: {}}}}
