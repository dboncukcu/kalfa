# Generated by kalfa 0.2.4 from examples/10_char_lm/config.yaml: the driver document kalfa.driver.recipe produces,
# the one the blocks of src/kalfa/contract.yaml open. Not written by hand; regenerate with:
#   uv run python tools/regenerate.py recipes
include: [../../../src/kalfa/contract.yaml]
losses:
  lm: {uri: /adapter/kalfa/criterion, params: {criterion: {uri: /criterion/kalfa/cross_entropy}}}
metrics:
  perplexity: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/kalfa/perplexity}}}
triggers:
  stop_0: {uri: /trigger/kalfa/plateau, params: {monitor: val/perplexity, patience: 10}}
plots:
  loss_curve: {uri: /plot/kalfa/loss_curve, params: {series: [train/lm, val/perplexity]}}
checkpoint: {uri: /checkpoint/kalfa/best, params: {monitor: val/perplexity}}
blocks:
  gpt:
    inputs: [input_ids]
    outputs: [logits]
    spec:
    - {uri: /layer/torch/embedding, params: {num: {uri: /data/kalfa/vocab_size}, dim: 256}}
    - {uri: /layer/gpt/gpt, params: {d_model: 256, layers: 4, heads: 4, seq_len: 128}}
    - {uri: /layer/kalfa/linear, params: {out_features: {uri: /data/kalfa/vocab_size}}}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/text_lines, params: {path: data/shakespeare.txt}}, filter_pre: [],
      filter_set: [], split: {uri: /split/kalfa/random, params: {ratios: [0.98, 0.02, 0.0], seed: 7}}, loaders: {train: {
          uri: /loader/kalfa/torch, set: train, params: {set: train, size: 64}}, valid: {uri: /loader/kalfa/torch, set: 
            valid, params: {set: valid, size: 64}}, test: {uri: /loader/kalfa/torch, set: test, params: {set: test, size: 
              64}}}, prep: {uri: /lego/kalfa/fit, params: {fields: {text: {preprocessors: [tokenizer]}}, preprocessors: {
            tokenizer: {uri: /pre/kalfa/char_tokenizer}}, drop: [], keys: {tokenizer: {}}}, inputs: {df: train_df}}, 
        preprocessors_keys: {tokenizer: {}}, feed: {uri: /feed/kalfa/next_token, params: {seq_len: 128}}}}
  models: {block: models, params: {trained_items: [{name: gpt, index: 0, init: null, trainable: true, weights: null}], 
        composite_items: [], ema_items: [], trained_refs: {gpt: gpt}, composite_refs: {}, ema_refs: {}, seed: 7, rng: {
          uri: /rng/kalfa/derived, params: {}}, builder: /builder/kalfa/module}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: main, uri: /optimizer/torch/adamw, params: {lr: 
              0.0003, weight_decay: 0.1}, loss: lm, schedule: {uri: /schedule/kalfa/warmup_cosine, params: {warmup: 500, 
                total: 20000}}, models: {gpt: gpt}}], optimizer_refs: {main: opt_main}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {}}, turn_params: {accumulate: 2, 
          amp: true, grad_clip: 1.0}, losses_keys: {lm: {target: targets}}, metrics_keys: {perplexity: {target: targets}},
      predicts: gpt, epochs: null, steps: {total: 20000, turn: 200}, rules: [], stop: ['@triggers.stop_0'], 
        history_prefix: {train: train, valid: val, test: test}}}
  after: {block: after, params: {report: best, figures: {uri: /lego/kalfa/figures, params: {}}, predicts: gpt, targets: {},
      generate: {uri: /generate/kalfa/lm_sampler, params: {model: gpt, prompt: 'ROMEO:', max_new_tokens: 500, 
            temperature: 0.9}}, plots_keys: {loss_curve: {lego: /plot/kalfa/loss_curve}}, plot_bus: {prep: prep, 
          train_loader: train_loader, valid_loader: valid_loader, test_loader: test_loader, composites: composites, 
          device: device, counters: counters_final, optimizers: optimizers_final, emas: emas_final, rules: rules_final, 
          losses: losses, losses_keys: losses_keys}, losses_keys: {lm: {target: targets}}}}
