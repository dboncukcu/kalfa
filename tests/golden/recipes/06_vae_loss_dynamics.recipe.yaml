# Generated by kalfa 0.2.4 from examples/06_vae_loss_dynamics/config.yaml: the driver document kalfa.driver.recipe produces,
# the one the blocks of src/kalfa/contract.yaml open. Not written by hand; regenerate with:
#   uv run python tools/regenerate.py recipes
include: [../../../src/kalfa/contract.yaml]
losses:
  vae_loss: {uri: /adapter/kalfa/objective, params: {objective: {uri: /objective/kalfa/vae, params: {encoder: encoder, 
            decoder: decoder, recon: {uri: /criterion/kalfa/mse}, w_rec: 1.0, kl_schedule: {uri: 
              /schedule/kalfa/linear_warmup, params: {start: 0.0, end: 1.0, steps: 20000}}}}}}
metrics:
  recon_rmse: {uri: /adapter/kalfa/metric, params: {metric: {uri: /metric/kalfa/rmse}}}
triggers:
  half_recon: {uri: /trigger/kalfa/after_turn, params: {at: 10}}
  to_huber: {uri: /trigger/kalfa/plateau, params: {monitor: val/recon_rmse, patience: 5}}
  stop_0: {uri: /trigger/kalfa/plateau, params: {monitor: val/recon_rmse, patience: 12}}
plots:
  loss_curve: {uri: /plot/kalfa/loss_curve, params: {series: [train/vae_loss/recon, train/vae_loss/kl, val/recon_rmse]}}
  reconstructions: {uri: /plot/kalfa/image_pairs, params: {n: 8}}
checkpoint: {uri: /checkpoint/kalfa/best, params: {monitor: val/recon_rmse}}
blocks:
  encoder:
    inputs: [image]
    outputs: [mu, logvar]
    graph:
      f: {uri: /layer/torch/flatten, inputs: [image]}
      h: {uri: /layer/kalfa/linear_relu, params: {out_features: 512}, inputs: [f]}
      mu: {uri: /layer/kalfa/linear, params: {out_features: 32}, inputs: [h]}
      logvar: {uri: /layer/kalfa/linear, params: {out_features: 32}, inputs: [h]}
  decoder:
    inputs: [z]
    outputs: [x_hat]
    spec:
    - {uri: /layer/kalfa/linear_relu, params: {out_features: 256}}
    - {uri: /layer/kalfa/linear, params: {out_features: 784}}
    - {uri: /layer/kalfa/unflatten, params: {shape: [1, 28, 28]}}
  vae:
    inputs: [image]
    outputs: [x_hat]
    graph:
      enc: {model: encoder, inputs: [image], outputs: [mu, logvar]}
      z: {uri: /layer/kalfa/reparam, inputs: [mu, logvar]}
      x_hat: {model: decoder, inputs: [z]}
flow:
  outputs: [history, predictions]
  data: {block: data, params: {source: {uri: /source/kalfa/image_folder, params: {path: data/mnist}}, filter_pre: [], 
        filter_set: [], split: {uri: /split/kalfa/random, params: {ratios: [0.9, 0.1, 0.0], seed: 7}}, loaders: {train: {
          uri: /loader/kalfa/torch, set: train, params: {set: train, size: 128}}, valid: {uri: /loader/kalfa/torch, set: 
            valid, params: {set: valid, size: 128}}, test: {uri: /loader/kalfa/torch, set: test, params: {set: test, 
              size: 128}}}, prep: {uri: /lego/kalfa/fit, params: {fields: {image: {preprocessors: [to_tensor]}}, 
            preprocessors: {to_tensor: {uri: /pre/kalfa/to_tensor}}, drop: [], keys: {to_tensor: {}}}, inputs: {df: 
            train_df}}, preprocessors_keys: {to_tensor: {}}, feed: {uri: /feed/kalfa/table, params: {}}}}
  models: {block: models, params: {trained_items: [{name: encoder, index: 0, init: null, trainable: true, weights: null},
        {name: decoder, index: 1, init: null, trainable: true, weights: null}], composite_items: [name: vae], ema_items: [],
      trained_refs: {encoder: encoder, decoder: decoder}, composite_refs: {vae: vae}, ema_refs: {}, seed: 7}}
  optimizers: {block: optimizers, params: {optimizer_items: [{name: main, uri: /optimizer/torch/adam, params: {lr: 0.001},
          loss: vae_loss, schedule: null, models: {encoder: encoder, decoder: decoder}}], optimizer_refs: {main: 
          opt_main}}}
  training: {block: training, params: {turn: {uri: /turn/kalfa/alternating, params: {}}, turn_params: {}, losses_keys: {
          vae_loss: {}}, metrics_keys: {recon_rmse: {target: input}}, predicts: vae, epochs: 50, steps: null, rules: [{
            name: half_recon, when: '@triggers.half_recon', set: {vae_loss.w_rec: 0.5}, after: null}, {name: to_huber, 
            when: '@triggers.to_huber', set: {vae_loss.recon: {uri: /criterion/kalfa/huber}}, after: null}], stop: ['@triggers.stop_0'],
      history_prefix: {train: train, valid: val, test: test}}}
  after: {block: after, params: {report: best, figures: {uri: /lego/kalfa/figures, params: {}}, predicts: vae, targets: {},
      generate: null, plots_keys: {loss_curve: {}, reconstructions: {}}}}
