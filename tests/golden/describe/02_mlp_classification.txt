config.yaml                                                                          kalfa 0.2.4
  layers      config.yaml, /alias/kalfa/base, /alias/kalfa/tabular
  params
    epochs  30
    lr      0.001
    seed    7
  seed        7
  device      cpu (no device key)
  rng         derived (no rng key)
  record      runs/churn_$datetime$

── DATA ────────────────────────────────────────────────────────────────────────────────────────
  source      parquet  churn.parquet                      2 000 rows · 7 columns
  split       random  0.7 / 0.15 / 0.15  seed=7           train 1 400  ·  valid 300  ·  test 300
  batch       256                                         feed  table

  field    columns                     preprocessors  role
  ───────────────────────────────────────────────────────────
  num_*    num_0, num_1, num_2, num_3  std_scaler     feature
  cat_*    cat_a, cat_b                onehot         feature
  churned  churned                     label          target

  churn.parquet ─→ split random
    ├─ train    1 400  ─→ fit std_scaler, onehot, label ─→ table
    ├─ valid      300  ─→ apply                         ─→ table
    └─ test       300  ─→ apply                         ─→ table

── MODEL ───────────────────────────────────────────────────────────────────────────────────────
  net   trained  ·  optimizer net
    x ─→ linear_relu 64 ─→ linear_relu 64 ─→ linear 2 ─→ logits

  the parameter counts need --measure

── TRAINING ────────────────────────────────────────────────────────────────────────────────────
  turn        alternating         30 epochs       predicts net

  optimizer  lego           trains  loss  schedule
  ───────────────────────────────────────────────────
  net        adam lr=0.001  ─→ net  ce    no schedule

  loss  lego                                reported on
  ──────────────────────────────────────────────────────────────────────────────
  ce    cross_entropy weight=class_weights  train, valid, test  active at turn 1

  metric    lego      reported on
  ────────────────────────────────────────
  accuracy  accuracy  train, valid, test
  f1        f1        train, valid, test

  checkpoint  best monitor=val/f1 mode=max                report best
  stop        val/f1 plateau 5

── AFTER ───────────────────────────────────────────────────────────────────────────────────────
  report      best        predict net ─→ predictions.parquet
  plots       loss_curve, confusion (confusion_matrix)  ─→ plots/
  record      runs/churn_$datetime$

── COLUMNS ─────────────────────────────────────────────────────────────────────────────────────
  column   dtype         field    preprocessors  role     tensor
  ───────────────────────────────────────────────────────────────
  num_0    double        num_*    std_scaler     feature  x
  num_1    double        num_*    std_scaler     feature  x
  num_2    double        num_*    std_scaler     feature  x
  num_3    double        num_*    std_scaler     feature  x
  cat_a    large_string  cat_*    onehot         feature  x
  cat_b    large_string  cat_*    onehot         feature  x
  churned  large_string  churned  label          target   churned
  the produced widths and the tensor slots need --measure
