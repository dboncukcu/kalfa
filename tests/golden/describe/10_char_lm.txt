config.yaml                                                                                                          kalfa 0.2.6
  layers      config.yaml, /alias/kalfa/base, /alias/kalfa/text
  params
    total_steps  20000     lr           0.0003    seed         7
    turn_steps   200       seq_len      128
  seed        7
  device      cuda
  rng         derived (no rng key)
  record      runs/charlm_$datetime$

── DATA ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  source      text_lines  data/shakespeare.txt            2 000 rows · 1 columns
  split       random  0.98 / 0.02 / 0  seed=7             train 1 960  ·  valid 40  ·  test 0
  batch       64                                          feed  next_token seq_len=128

  field  columns  preprocessors  role
  ──────────────────────────────────────
  text   text     tokenizer      feature

  data/shakespeare.txt ─→ split random
    ├─ train    1 960  ─→ fit tokenizer ─→ next_token
    ├─ valid       40  ─→ apply         ─→ next_token
    └─ test         0  ─→ apply         ─→ next_token

── MODEL ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  gpt   trained  ·  optimizer main
    input_ids ─→ embedding num=vocab_size dim=256 ─→ gpt d_model=256 layers=4 heads=4 seq_len=128 ─→ linear vocab_size ─→ logits

  the parameter counts need --measure

── TRAINING ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  turn        alternating         20000 steps of 200  predicts gpt
              accumulate=2 amp=true grad_clip=1

  optimizer  lego                              trains  loss  schedule
  ───────────────────────────────────────────────────────────────────────────────────────────────
  main       adamw lr=0.0003 weight_decay=0.1  ─→ gpt  lm    warmup_cosine warmup=500 total=20000

  loss  lego           compares      reported on
  ───────────────────────────────────────────────────────────────────────
  lm    cross_entropy  — ─→ targets  train, valid, test  active at turn 1

  metric      lego        compares      reported on
  ──────────────────────────────────────────────────────────
  perplexity  perplexity  — ─→ targets  train, valid, test

  checkpoint  best monitor=val/perplexity                 report best
  stop        val/perplexity plateau 10

── AFTER ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  report      best        predict gpt ─→ predictions.parquet
  plots       loss_curve  ─→ plots/
  generate    lm_sampler model=gpt prompt=ROMEO: max_new_tokens=500 temperature=0.9  ─→ samples/
  record      runs/charlm_$datetime$

── COLUMNS ─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  column  dtype   field  preprocessors  role     tensor
  ─────────────────────────────────────────────────────
  text    string  text   tokenizer      feature  x
  the produced widths and the tensor slots need --measure
